using System;

class program
{
    static void main()
    {
        int A = 2;
        int B = 5;
        int C = 8;

        bool result = (A < B) && (B < C);

        Console.WriteLine(result);
    }
}