using System;

class program
{
    static void main()
    {
        int A = -1;
        int B = -5;

        bool result = (A >= 0) || (B < -2);

        Console.WriteLine(result);

    }
}